/**
 * Placeholder so `tsc --noEmit` has something to type-check before the
 * Next.js app scaffold lands in tranche 3.
 *
 * Delete this file when you scaffold the App Router (the first real
 * page.tsx replaces it).
 */
export const TRANCHE = 1 as const;
